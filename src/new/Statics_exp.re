let extend_let_body_ctx = (_, _, _) => failwith("todo");

/*
 open Term_exp;
 let syn = (info: TypeInfo_exp.t, e: Term_exp.t) =>
   e
   |> Term.get(
     fun
     | OpHole => Type.Hole
     | Num(_) => failwith("todo")
   );
 */
let syn = (_, _) => failwith("todo");
